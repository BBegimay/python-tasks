To run a tests please run in this directory

```
python3 -m pytest -v --driver Chrome --driver-path ${PATH_TO_DRIVER} tests_labirint/tests_labirint.py
```

Where `${PATH_TO_DRIVER}` is the path for Selenium Driver for the current OS